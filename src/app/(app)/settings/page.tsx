import { RoutePlaceholder } from "../../_components/route-placeholder";

export default function SettingsPage() {
  return (
    <RoutePlaceholder
      title="Profile Settings"
      description="Placeholder route for future profile, level, and account settings."
    />
  );
}
