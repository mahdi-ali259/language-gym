import { RoutePlaceholder } from "../_components/route-placeholder";

export default function LandingPage() {
  return (
    <RoutePlaceholder
      title="Public Landing Page"
      description="Placeholder route for the future public entry point and guest practice call to action."
    />
  );
}
