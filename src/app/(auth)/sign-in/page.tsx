import { RoutePlaceholder } from "../../_components/route-placeholder";

export default function SignInPage() {
  return (
    <RoutePlaceholder
      title="Sign In"
      description="Placeholder route for Google sign-in setup in a future phase."
    />
  );
}
